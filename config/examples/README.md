# Examples

Use `discovery/config.example.yml` as the canonical deployment example. It stays
at that path so existing installation instructions and automation continue to
work. Examples must contain placeholders, never credentials.
