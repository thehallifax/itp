{
  "annotations": {"list": []},
  "description": "ITP-managed overview of canonical telemetry produced by the SNMP collector.",
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"},
      "fieldConfig": {"defaults": {"color": {"mode": "thresholds"}, "mappings": [], "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}]}}, "overrides": []},
      "gridPos": {"h": 4, "w": 8, "x": 0, "y": 0},
      "id": 1,
      "options": {"colorMode": "background", "graphMode": "none", "justifyMode": "center", "orientation": "auto", "reduceOptions": {"calcs": ["lastNotNull"], "fields": "/^Devices$/", "values": false}, "textMode": "auto", "wideLayout": true},
      "targets": [{"datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"}, "format": "table", "rawQuery": true, "rawSql": "SELECT COUNT(*) AS \"Devices\"\nFROM (\n  SELECT hostname\n  FROM device\n  WHERE time >= $__timeFrom AND time <= $__timeTo\n    AND collector = 'snmp'\n  GROUP BY hostname\n)", "refId": "A"}],
      "title": "Discovered Devices",
      "type": "stat"
    },
    {
      "datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"},
      "fieldConfig": {"defaults": {"color": {"mode": "thresholds"}, "mappings": [], "thresholds": {"mode": "absolute", "steps": [{"color": "red", "value": null}, {"color": "green", "value": 1}]}}, "overrides": []},
      "gridPos": {"h": 4, "w": 8, "x": 8, "y": 0},
      "id": 2,
      "options": {"colorMode": "background", "graphMode": "none", "justifyMode": "center", "orientation": "auto", "reduceOptions": {"calcs": ["lastNotNull"], "fields": "/^Available$/", "values": false}, "textMode": "auto", "wideLayout": true},
      "targets": [{"datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"}, "format": "table", "rawQuery": true, "rawSql": "WITH latest AS (\n  SELECT hostname, available, ROW_NUMBER() OVER (PARTITION BY hostname ORDER BY time DESC) AS row_number\n  FROM availability\n  WHERE time >= $__timeFrom AND time <= $__timeTo\n    AND collector = 'snmp'\n)\nSELECT SUM(CASE WHEN available THEN 1 ELSE 0 END) AS \"Available\"\nFROM latest\nWHERE row_number = 1", "refId": "A"}],
      "title": "Devices Available",
      "type": "stat"
    },
    {
      "datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"},
      "fieldConfig": {"defaults": {"color": {"mode": "thresholds"}, "mappings": [], "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}, {"color": "red", "value": 1}]}}, "overrides": []},
      "gridPos": {"h": 4, "w": 8, "x": 16, "y": 0},
      "id": 3,
      "options": {"colorMode": "background", "graphMode": "none", "justifyMode": "center", "orientation": "auto", "reduceOptions": {"calcs": ["lastNotNull"], "fields": "/^Unavailable$/", "values": false}, "textMode": "auto", "wideLayout": true},
      "targets": [{"datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"}, "format": "table", "rawQuery": true, "rawSql": "WITH latest AS (\n  SELECT hostname, available, ROW_NUMBER() OVER (PARTITION BY hostname ORDER BY time DESC) AS row_number\n  FROM availability\n  WHERE time >= $__timeFrom AND time <= $__timeTo\n    AND collector = 'snmp'\n)\nSELECT SUM(CASE WHEN available THEN 0 ELSE 1 END) AS \"Unavailable\"\nFROM latest\nWHERE row_number = 1", "refId": "A"}],
      "title": "Devices Unavailable",
      "type": "stat"
    },
    {
      "datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"},
      "fieldConfig": {"defaults": {"custom": {"align": "auto", "cellOptions": {"type": "auto"}, "inspect": false}}, "overrides": []},
      "gridPos": {"h": 9, "w": 24, "x": 0, "y": 4},
      "id": 4,
      "options": {"cellHeight": "sm", "footer": {"countRows": false, "fields": "", "reducer": ["sum"], "show": false}, "showHeader": true},
      "targets": [{"datasource": {"type": "influxdb", "uid": "ffsu5ap2kr5dse"}, "format": "table", "rawQuery": true, "rawSql": "WITH latest AS (\n  SELECT time, hostname, device_ip, platform, vendor, description, site, customer,\n         ROW_NUMBER() OVER (PARTITION BY hostname ORDER BY time DESC) AS row_number\n  FROM device\n  WHERE time >= $__timeFrom AND time <= $__timeTo\n    AND collector = 'snmp'\n)\nSELECT hostname AS \"Hostname\", device_ip AS \"Management IP\", platform AS \"Type\",\n       vendor AS \"Vendor\", description AS \"Description\", customer AS \"Customer\", site AS \"Site\", time AS \"Last Seen\"\nFROM latest\nWHERE row_number = 1\nORDER BY hostname", "refId": "A"}],
      "title": "SNMP Inventory",
      "type": "table"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 41,
  "tags": ["itp", "itp-managed", "snmp"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "timepicker": {},
  "timezone": "browser",
  "title": "SNMP Infrastructure Overview",
  "uid": "itp-snmp-overview",
  "version": 1,
  "weekStart": ""
}
