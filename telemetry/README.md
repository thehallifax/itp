# Telemetry

This package is reserved for shared telemetry contracts, normalisation helpers,
and validation used after a collector has adapted vendor responses. It must not
depend on vendor collector packages.
